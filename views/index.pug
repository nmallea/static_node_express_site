extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio

      p.lead.text-light
        | From a simple random quote generator written in vanilla JavaScript to a static Node.js site, this portfolio showcases some of the projects I have created. More projects coming soon.
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=project.image_urls[0])
            h5 #{project.project_name}

